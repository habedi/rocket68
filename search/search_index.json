{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Rocket 68","text":"<p>Rocket 68 is a modern, fast, and cycle-accurate emulator core for the Motorola 68000 microprocessor, written entirely in standard C11. </p> <p>Designed for both embedded systems and advanced multi-threaded emulation environments, Rocket 68 prioritizes cache locality, safety, and architectural decoupling to serve as the perfect powerhouse for your next retro console or homebrew project.</p>"},{"location":"#features","title":"Features","text":"<ul> <li>Strict C11 Modernization: Built using standard <code>&lt;assert.h&gt;</code> sizing checks, anonymous register unions for safe endianness abstraction, and type-safe <code>_Generic</code> macros for clean memory access.</li> <li>Architecturally Decoupled: Unlike older emulators that rely on messy global states, Rocket 68 explicitly passes an <code>M68kCpu*</code> context instance to every single cycle and I/O callback. You can securely run a dozen virtual machines in the same process on different threads without cross-contamination.</li> <li>Cache Aligned: The core <code>M68kCpu</code> struct is enforced with 64-byte <code>__attribute__((aligned(64)))</code> boundary padding to prevent cache-line thrashing and hardware penalties during the hot emulation loop.</li> <li>Cycle-Accurate Precision: Carefully tested timings and execution sequences mirroring the exact pipeline delays of real M68k silicon.</li> <li>Save State Ready: Native context block serialization for rapid game freeze and resume capabilities.</li> </ul>"},{"location":"#why-c11","title":"Why C11?","text":"<p>Rocket 68 was heavily refactored from legacy C99 definitions into standardized C11 functionality. This allows developers to construct concurrent emulation environments using <code>&lt;stdatomic.h&gt;</code> and <code>_Thread_local</code> without worrying about legacy compiler undefined behaviors or clashing global execution vectors.</p>"},{"location":"#next-steps","title":"Next Steps","text":"<p>Head over to the Getting Started guide to learn how to hook the emulator up to your own memory maps and I/O bus!</p>"},{"location":"api-reference/","title":"API Reference","text":"<p>The primary interaction with Rocket 68 revolves around the <code>M68kCpu</code> struct and its management functions. Since Rocket is purely decoupled, all API signatures require a pointer to the context being emulated.</p>"},{"location":"api-reference/#core-structures","title":"Core Structures","text":""},{"location":"api-reference/#m68kcpu","title":"<code>M68kCpu</code>","text":"<p>The <code>M68kCpu</code> is a 64-byte aligned data structure representing the entire state of a single Motorola 68000 microchip.</p> <pre><code>typedef struct M68kCpu {\n    M68kRegister d_regs[8]; // Data registers D0-D7\n    M68kRegister a_regs[8]; // Address registers A0-A7\n    u32 pc;                 // Program Counter\n    u16 sr;                 // Status Register\n    // ... Internal execution state\n} __attribute__((aligned(64))) M68kCpu;\n</code></pre>"},{"location":"api-reference/#m68kregister","title":"<code>M68kRegister</code>","text":"<p>A standard C11 anonymous union used to quickly extract 16-bit or 32-bit components natively based on platform endianness. <pre><code>typedef union {\n    u32 l;\n    struct {\n        u16 w;\n    };\n} M68kRegister;\n</code></pre> Usage: - <code>cpu-&gt;d_regs[0].l = 0xFFFF; // Write 32 bits</code> - <code>cpu-&gt;d_regs[0].w = 0x00FF; // Write lower 16 bits</code></p>"},{"location":"api-reference/#functions","title":"Functions","text":""},{"location":"api-reference/#m68k_init","title":"<code>m68k_init</code>","text":"<p><pre><code>void m68k_init(M68kCpu* cpu, u8* memory, u32 memory_size);\n</code></pre> Initializes the CPU struct and binds it to a standard flat byte array representing physical RAM boundaries.</p>"},{"location":"api-reference/#m68k_reset","title":"<code>m68k_reset</code>","text":"<p><pre><code>void m68k_reset(M68kCpu* cpu);\n</code></pre> Executes a hard reset. Populates the <code>pc</code> and <code>ssp</code> registers by fetching 32-bit vectors from address <code>0x00</code> and <code>0x04</code> respectively.</p>"},{"location":"api-reference/#m68k_execute","title":"<code>m68k_execute</code>","text":"<p><pre><code>int m68k_execute(M68kCpu* cpu, int cycles);\n</code></pre> Burns CPU cycles by executing instructions. The emulator will run until <code>cycles</code> drops to or below zero. Returns the total number of cycles actually consumed.</p>"},{"location":"api-reference/#io-callbacks","title":"I/O Callbacks","text":"<p>Rocket 68 achieves architectural decoupling through callbacks installed directly on the CPU structure. None of these are globally bound.</p> <pre><code>// Emulate wait states on the bus\nvoid m68k_set_wait_bus_callback(M68kCpu* cpu, void (*fn)(M68kCpu* c, u32 addr, M68kSize sz));\n\n// Respond to Interrupt Acknowledge cycles\nvoid m68k_set_int_ack_callback(M68kCpu* cpu, int (*fn)(M68kCpu* c, int level));\n\n// Respond to Function Code (FC) changes\nvoid m68k_set_fc_callback(M68kCpu* cpu, void (*fn)(M68kCpu* c, unsigned int fc));\n\n// Trace PC changes\nvoid m68k_set_pc_changed_callback(M68kCpu* cpu, void (*fn)(M68kCpu* c, u32 pc));\n</code></pre>"},{"location":"examples/","title":"Real-World Scenario","text":"<p>Below is an example of setting up a tiny, self-contained run cycle for <code>rocket68</code>. In a real emulator like an Amiga or Sega Genesis, the memory map handler <code>m68k_read_32</code> or <code>my_wait_bus</code> would direct memory chunks to video RAM, cartridges, or sound chips instead of a flat RAM allocation!</p> <pre><code>#include &lt;stdio.h&gt;\n#include &lt;stdlib.h&gt;\n#include &lt;string.h&gt;\n#include \"m68k.h\"\n\n// 1) Define a simple interrupt system\nint my_int_ack(M68kCpu* cpu, int level) {\n    printf(\"Acknowledged CPU interrupt at level %d!\\n\", level);\n    return M68K_INT_ACK_AUTOVECTOR;\n}\n\n// 2) Define illegal instruction traps\nint my_illg_cb(M68kCpu* cpu, int opcode) {\n    printf(\"FATAL: CPU halted on opcode 0x%04X at PC:%08X\\n\", opcode, cpu-&gt;pc);\n    return 1; // Halt\n}\n\nint main() {\n    u32 memory_size = 64 * 1024; // 64 KB\n    u8* ram = calloc(memory_size, 1);\n\n    // Mock an instruction at address 0x1000:\n    // 0x1000: NOP (0x4e71)\n    // 0x1002: STOP #$2000 (0x4e72, 0x2000)\n    ram[0x1000] = 0x4e; ram[0x1001] = 0x71; \n    ram[0x1002] = 0x4e; ram[0x1003] = 0x72; \n    ram[0x1004] = 0x20; ram[0x1005] = 0x00;\n\n    M68kCpu core;\n    m68k_init(&amp;core, ram, memory_size);\n\n    // Attach callbacks\n    m68k_set_int_ack_callback(&amp;core, my_int_ack);\n    m68k_set_illg_callback(&amp;core, my_illg_cb);\n\n    // Simulate bootloader dropping us at 0x1000\n    m68k_set_pc(&amp;core, 0x1000);\n    m68k_set_sr(&amp;core, 0x2700); // Supervisor, interrupts masked\n\n    int cycles_burned = m68k_execute(&amp;core, 100);\n\n    printf(\"Executed %d cycles. CPU PC is now 0x%08X\\n\", cycles_burned, core.pc);\n    free(ram);\n    return 0;\n}\n</code></pre>"},{"location":"getting-started/","title":"Getting Started","text":"<p>Rocket 68 is designed to be embedded directly into your C or C++ projects. </p>"},{"location":"getting-started/#1-integration","title":"1. Integration","text":"<p>The simplest way to use Rocket 68 is to just drop the source files into your build system.</p> <p>Required files: - <code>include/m68k.h</code> (The primary API header) - <code>src/m68k/*.c</code> (The core execution units and opcodes)</p> <p>Compile it with any C11-compliant compiler: <pre><code>gcc -std=c11 -O3 -c src/m68k/*.c\n</code></pre></p>"},{"location":"getting-started/#2-initialization","title":"2. Initialization","text":"<p>Before the emulator can run, you must instantiate the <code>M68kCpu</code> context object and provide it with memory.</p> <pre><code>#include \"m68k.h\"\n#include &lt;stdlib.h&gt;\n\nint main() {\n    // 1. Allocate your memory map (e.g., 16 MB of RAM)\n    u32 mem_size = 16 * 1024 * 1024;\n    u8* ram = malloc(mem_size);\n\n    // 2. Instantiate the CPU state\n    M68kCpu cpu;\n\n    // 3. Initialize the emulator internals\n    m68k_init(&amp;cpu, ram, mem_size);\n\n    return 0;\n}\n</code></pre>"},{"location":"getting-started/#3-hooking-up-the-io-bus","title":"3. Hooking up the I/O Bus","text":"<p>Rocket 68 knows how to execute instructions, but it relies on your host application to handle physical hardware mapping. You must wire up the callbacks to interface with the outside world.</p> <p>Because Rocket 68 is fully decoupled, every callback function takes the <code>cpu</code> instance as its first argument.</p> <pre><code>// Define your custom bus logic\nvoid my_wait_bus(M68kCpu* cpu, u32 address, M68kSize size) {\n    // Determine how many cycles were burned accessing this memory\n    cpu-&gt;cycles_remaining -= 2; \n}\n\nint main() {\n    // ... setup ...\n\n    m68k_set_wait_bus_callback(&amp;cpu, my_wait_bus);\n\n    // ... run ...\n}\n</code></pre>"},{"location":"getting-started/#4-execution","title":"4. Execution","text":"<p>Once initialized, the emulator simply fetches and executes instructions in a loop. A \"Reset\" must be issued first to boot the CPU from its reset vectors (Address <code>0x00</code> and <code>0x04</code>).</p> <pre><code>// Force a hardware reset to load PC and SSP\nm68k_reset(&amp;cpu);\n\n// Execute roughly 100,000 cycles\nint executed = m68k_execute(&amp;cpu, 100000);\n</code></pre>"}]}